# Optional Demo Data

`seed_demo_data.php` adds realistic LMS demo records for local testing and presentations.

Run from the project root:

```sh
php database/seeds/seed_demo_data.php
```

The app does not run this file automatically.
